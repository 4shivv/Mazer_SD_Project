services:
  api:
    build:
      context: ./apps/api
    command: npm run dev
    environment:
      - PORT=4000
      - OLLAMA_HOST=http://ollama:11434
      - OLLAMA_MODEL=llama3:8b-instruct-q4_K_M
      - OLLAMA_ALLOWED_CHAT_MODELS=llama3:8b-instruct-q4_K_M,mistral:7b-instruct-q4_0,llama3.1:8b-instruct-q4_K_M
      - OLLAMA_EMBED_MODEL=nomic-embed-text
      - CHROMA_HOST=http://chroma:8000
      - CHROMA_COLLECTION=ew_training_materials
      - CHROMA_TENANT=default_tenant
      - CHROMA_DATABASE=default_database
      - MONGO_URL=mongodb://mongo:27017/mazerai
      - JWT_SECRET=dummysecret
    ports:
      - "4000:4000"
    volumes:
      - ./apps/api:/usr/src/app
      # keep node_modules inside the container so host mount doesn't wipe it
      - /usr/src/app/node_modules
    working_dir: /usr/src/app
    depends_on:
      - ollama
      - mongo
      - chroma

  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  chroma:
    image: chromadb/chroma:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - chroma_data:/chroma

volumes:
  ollama_data:
  mongo_data:
  chroma_data:
